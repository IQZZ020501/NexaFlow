name: CI

on:
  pull_request:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  backend:
    name: Backend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        run: python -m pip install uv

      - name: Install dependencies
        run: uv sync --dev --frozen

      - name: Compile Python
        run: uv run python -m compileall nexaflow tests main.py

      - name: Run identity tests
        run: uv run python -m tests.identity

      - name: Run workspace tests
        run: uv run python -m tests.workspaces

      - name: Run team tests
        run: uv run python -m tests.teams

      - name: Run knowledge tests
        run: uv run python -m tests.knowledge

      - name: Run llm tests
        run: uv run python -m tests.llm

      - name: Run main smoke tests
        run: uv run python -m tests.test_main

  frontend:
    name: Frontend
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Typecheck
        run: bun run typecheck

      - name: Lint
        run: bun run lint

      - name: Test
        run: bun test

      - name: Build
        run: bun run build
